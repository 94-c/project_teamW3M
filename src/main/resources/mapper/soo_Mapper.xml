<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="InquiryDAO">
	<insert id="insertInquiry" parameterType="inquiry">
		INSERT INTO inquiry (inq_seq, inq_title, inq_writer, inq_content)
		VALUES(nextval('inq_user_seq'), #{inq_title}, #{inq_writer}, #{inq_content})
	</insert>
	<select id="getInquiryList" resultType="com.spring.w3m.inquiry.user.vo.InquiryVO">
		SELECT inq_seq, inq_title, inq_writer, inq_cnt FROM inquiry ORDERED BY inq_seq DESC
	</select>	
</mapper>

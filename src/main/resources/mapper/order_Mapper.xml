<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="OrderDAO">

	
	<insert id="order_inser_prod" parameterType="OrderVO">
	INSERT INTO order_prod (order_prod_seq,user_id,prod_code,prod_amount ) values(nextval('order_prod_seq'),#{user_id},#{prod_code},#{prod_amount})
	</insert>
	
	<select id="order_page_go" parameterType="OrderVO" resultType="OrderVO">
	SELECT p.prod_title_image, p.prod_title, p.prod_seq, p.prod_price, p.prod_point,p.prod_code,u.user_id,
	u.user_name, u.user_email, u.user_phone,u.user_zipcode,u.user_address1,u.user_address2
	from product p,user_member u
	where p.prod_code = #{prod_code} and u.user_id = #{user_id};
	</select>
</mapper>
